@{
    ViewData["Title"] = "Access Denied";
    Layout = "_AuthLayout";
}

<div class="auth-card text-center">
    <div class="auth-icon bg-danger mx-auto"><i class="bi bi-shield-exclamation"></i></div>
    <h1 class="h4 fw-bold mt-3 mb-1">Access Denied</h1>
    <p class="text-muted small mb-4">You don't have permission to view this page.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary py-2 fw-semibold">
        <i class="bi bi-house me-1"></i> Back to Home
    </a>
</div>