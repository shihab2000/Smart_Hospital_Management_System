@model SHMS.Models.PrescriptionItem

@{
    ViewData["Title"] = "Create";
}

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="card border-0 shadow-sm rounded-4">
            <div class="card-body p-4 p-md-5">

                <div class="d-flex align-items-center gap-3 mb-4">
                    <div class="d-flex align-items-center justify-content-center rounded-3 text-white"
                         style="width:48px;height:48px;background:linear-gradient(135deg, var(--brand-secondary), var(--brand-primary));">
                        <i class="bi bi-capsule fs-5"></i>
                    </div>
                    <div>
                        <h1 class="h4 mb-0 fw-bold">Add Prescription Item</h1>
                        <p class="text-muted small mb-0">Add a medicine to an existing prescription.</p>
                    </div>
                </div>

                <hr class="mb-4" />

                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 small"></div>

                    <div class="row g-3">
                        <div class="col-md-6">
                            <div class="form-floating">
                                <select asp-for="PrescriptionId" class="form-select" asp-items="ViewBag.PrescriptionId">
                                    <option value="">-- Select Prescription --</option>
                                </select>
                                <label asp-for="PrescriptionId"></label>
                                <span asp-validation-for="PrescriptionId" class="text-danger small"></span>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="form-floating">
                                <select asp-for="MedicineId" class="form-select" asp-items="ViewBag.MedicineId">
                                    <option value="">-- Select Medicine --</option>
                                </select>
                                <label asp-for="MedicineId"></label>
                                <span asp-validation-for="MedicineId" class="text-danger small"></span>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="form-floating">
                                <input asp-for="Dosage" class="form-control" placeholder="Dosage" />
                                <label asp-for="Dosage"></label>
                                <span asp-validation-for="Dosage" class="text-danger small"></span>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="form-floating">
                                <input asp-for="Duration" class="form-control" placeholder="Duration" />
                                <label asp-for="Duration"></label>
                                <span asp-validation-for="Duration" class="text-danger small"></span>
                            </div>
                        </div>

                        <div class="col-12">
                            <div class="form-floating">
                                <textarea asp-for="Instructions" class="form-control" placeholder="Instructions" style="height:80px"></textarea>
                                <label asp-for="Instructions"></label>
                                <span asp-validation-for="Instructions" class="text-danger small"></span>
                            </div>
                        </div>
                    </div>

                    <div class="d-flex gap-2 mt-4">
                        <button type="submit" class="btn btn-primary flex-fill py-2 fw-semibold">
                            <i class="bi bi-check-circle me-1"></i> Create
                        </button>
                        <a asp-action="Index" class="btn btn-outline-secondary py-2">
                            <i class="bi bi-x-lg"></i>
                        </a>
                    </div>
                </form>

            </div>
        </div>

        <div class="text-center mt-3">
            <a asp-action="Index" class="text-decoration-none small">
                <i class="bi bi-arrow-left me-1"></i>Back to List
            </a>
        </div>
    </div>
</div>

<style>
    .btn-primary {
        background: linear-gradient(90deg, var(--brand-secondary), var(--brand-primary));
        border: none;
    }

    .btn-primary:hover,
    .btn-primary:focus {
        opacity: .92;
        background: linear-gradient(90deg, var(--brand-secondary), var(--brand-primary));
    }

    .form-control:focus,
    .form-select:focus {
        border-color: var(--brand-primary);
        box-shadow: 0 0 0 .2rem rgba(13, 148, 136, .15);
    }
</style>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}